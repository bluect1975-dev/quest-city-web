# Groups of paths explicitly accepted as intentional byte-identical
# duplicates, one comma-separated group per line. These are shared
# per-package boilerplate config files that are intentionally identical
# across sibling apps/packages in this monorepo (not accidental copies),
# and are not worth extracting into a common file at WEB-M0's scale.
apps/api/eslint.config.mjs, apps/dashboard/eslint.config.mjs, apps/student-web/eslint.config.mjs
apps/api/next-env.d.ts, apps/dashboard/next-env.d.ts, apps/student-web/next-env.d.ts
apps/api/tsconfig.json, apps/dashboard/tsconfig.json, apps/student-web/tsconfig.json
apps/dashboard/next.config.ts, apps/student-web/next.config.ts
# R3B-FIX (2026-08-08): the three groups below were previously split
# (WEB-M3B-FIX) based on a working-tree byte comparison that was itself
# contaminated by this Windows checkout's `core.autocrlf=true` — different
# local checkouts of the identical commit can disagree on CRLF/LF in the
# working tree, which is exactly what happened. Re-verified the only
# checkout-independent way: `git show HEAD:<path> | sha256sum` against the
# actual committed blob, which every fresh clone and CI will see
# regardless of local autocrlf settings. All three groups are genuinely
# fully byte-identical at the committed-blob level; the prior split
# undercounted them.
# GLPC-LAUNCH-ENFORCEMENT-FIX (2026-08-18): packages/student-support was
# added by an earlier session (Student Support Roles) with the same
# boilerplate tsconfig.json/vitest.config.ts as every other sibling
# package, but was never added to the two groups below -- a pre-existing
# allowlist omission, unrelated to and predating this change set, caught
# only because it blocks `verify.sh`'s mandatory check:duplicates gate.
# Re-verified genuinely byte-identical (`diff`) before adding, same
# discipline as the R3B-FIX note above.
packages/attempts/tsconfig.json, packages/content-runtime/tsconfig.json, packages/content-schema/tsconfig.json, packages/contracts/tsconfig.json, packages/convergence/tsconfig.json, packages/i18n/tsconfig.json, packages/identity/tsconfig.json, packages/learning-engines/tsconfig.json, packages/platform-admin/tsconfig.json, packages/staff-identity/tsconfig.json, packages/student-support/tsconfig.json, packages/telemetry/tsconfig.json, packages/test-fixtures/tsconfig.json, packages/theme-system/tsconfig.json
packages/attempts/vitest.config.ts, packages/content-runtime/vitest.config.ts, packages/content-schema/vitest.config.ts, packages/convergence/vitest.config.ts, packages/i18n/vitest.config.ts, packages/identity/vitest.config.ts, packages/learning-engines/vitest.config.ts, packages/platform-admin/vitest.config.ts, packages/staff-identity/vitest.config.ts, packages/student-support/vitest.config.ts, packages/telemetry/vitest.config.ts, packages/test-fixtures/vitest.config.ts, packages/theme-system/vitest.config.ts
packages/attempts/src/repository/types.ts, packages/convergence/src/repository/types.ts, packages/identity/src/repository/types.ts, packages/platform-admin/src/repository/types.ts, packages/staff-identity/src/repository/types.ts
# Single-line `/// <reference types="@testing-library/jest-dom/vitest" />`
# stub (WEB-M3B FVR), needed inside each package's own tsconfig `include`
# scope — same per-package-boilerplate rationale as the rest of this file.
apps/dashboard/vitest.d.ts, packages/ui/src/vitest.d.ts
