# Browser E2E tests

IMPLEMENT_LATER. Playwright-based multi-browser E2E tests (`05_01` toolchain decision; `07_05 §17`) apply once `/w` and `/dashboard` have real interactive flows to exercise. At WEB-M0 both routes are static placeholders, covered instead by `tests/integration/health-endpoints.test.ts`.
