# Test fixtures

Shared technical fixtures (e.g. the Balance Machine `RUNTIME_FIXTURE_BUNDLE`, 07_09 §25) live in `packages/test-fixtures` so they can be imported as a normal workspace dependency by both unit and integration tests, rather than duplicated as static files here.
