{
  "name": "@quest-city-web/identity",
  "version": "0.1.0",
  "private": true,
  "description": "WEB-M1 Phase 2: canonical student identity, class-code/alias/PIN login, session lifecycle, FIXED_WINDOW rate limiting and audit (02_25 §6.1/§6.10/§6.11, 02_26 §30, AGENTS.md WEB-M1 implementation baseline). Implements the shared backend model, not a Web-local identity system.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --config ../config/eslint/base.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "8.13.1"
  },
  "devDependencies": {
    "@types/node": "22.10.7",
    "@types/pg": "8.11.10",
    "eslint": "9.18.0",
    "typescript": "5.7.3",
    "vitest": "3.2.7"
  }
}
