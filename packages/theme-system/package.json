{
  "name": "@quest-city-web/theme-system",
  "version": "0.1.0",
  "private": true,
  "description": "Theme Package contract and QC-THEME-CORE fallback (07_01_02 §6). QC-THEME-CORE must always be available; validators and reward policy must never depend on theme.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --config ../config/eslint/base.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "9.18.0",
    "typescript": "5.7.3",
    "vitest": "3.0.4"
  }
}
