{
  "name": "@quest-city-web/learning-engines",
  "version": "0.1.0",
  "private": true,
  "description": "Engine registry contract (07_05 §10.1, 07_10). WEB-M0 scope: registry types and an empty registry only — no engine is implemented; that is Step 5 (07_10 §28), well past bootstrap.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --config ../config/eslint/base.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "9.18.0",
    "typescript": "5.7.3",
    "vitest": "3.0.4"
  }
}
