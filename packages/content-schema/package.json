{
  "name": "@quest-city-web/content-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Fixture-stage JSON Schemas for Web content bundles and runtime contracts (07_09 §26). No production curriculum content — schemas and fixtures only, per WEB-M0 scope.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --config ../config/eslint/base.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  },
  "devDependencies": {
    "eslint": "9.18.0",
    "typescript": "5.7.3",
    "vitest": "3.2.7"
  }
}
